<   : redirige l'entrée à partir d'un fichier (stdin).
ex: cat < salut.txt
    Envoi le contenu de salut.txt à la commande cat, qui l'affichera à l'écran.

>   : redirige la sortie vers un fichier (écrase le fichier existant)(stdout).
ex: echo "Bonjour" > salut.txt 
    Va écrire "Bonjour" dans le fichier salut.txt. Si le fichier existe déjà, il sera remplacé par ce nouveau contenu.

>>  : redirige la sortie vers un fichier (ajoute à la fin du fichier).
ex: echo "Salut encore" >> salut.txt
    Ajoute "Salut encore" à la fin de salut.txt sans effacer le contenu existant.

<<  : redirige l'entrée à partir d'un texte inline jusqu'à un délimiteur (heredoc).
ex: cat << FIN
    Ceci est un texte
    sur plusieurs lignes
    FIN
    Tout ce qui est entre << FIN et FIN est envoyé en tant qu'entrée standard à la commande cat. C'est souvent utilisé pour insérer du texte multi-lignes sans fichier.
______________________________________________

-> condition if n_red != 0 alors regarder si oud_red ou in_red.
   que dois-je parcourir pour ->prev?

   Si ?->prev == >
   Si ?->prev == <

   comment dois-je parcourir pour voir si Heredoc? comment ca fonctionne?

   comment on gère les built-in? doit-on simplement recréer son fonctionnement étape par étape ou alors dois-je faire comme pour les cmd?

______________________________________________

